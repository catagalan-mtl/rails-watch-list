<h1>Add movies to your list</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>
<%= link_to "Back to List", to: list_path(@list) %>
